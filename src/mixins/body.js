import { createNamespacedHelpers } from 'vuex';

const {
  mapState, mapMutations, mapActions, mapGetters,
} = createNamespacedHelpers('body');

export default {
  directives: {},
  data() {
    return {};
  },
  created() {},
  mounted() {},
  beforeDestroy() {},
  methods: {
    ...mapState([
    ]),
    ...mapMutations([
    ]),
    ...mapActions([
    ]),
    ...mapGetters([
    ]),
  },
  computed: {
  },
};
